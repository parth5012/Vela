# 07 — [E2E] Multi-Agent Integration Verification

**What to build:**
Create end-to-end integration tests validating multi-agent interactions via the mobile streaming API endpoint. Assert that switching active agents dynamically changes the system prompts loaded, binds the correct subset of registry tools, and triggers authentication redirects on unauthorized calls.

**Blocked by:** 05 — [Tools] Gmail Agent Tool Set & Binding, 06 — [Tools] Google Calendar Agent Tool Set & Binding

**Status:** ready-for-agent

## Acceptance Criteria
- [ ] Integration tests cover selecting different agents via the `/chat/message` API path.
- [ ] The test asserts only appropriate tools are exposed to the LLM during execution.
- [ ] Integration test triggers a mock auth redirection when Google credentials are missing.
- [ ] Platform bot gateways are verified to remain locked to the personal assistant setup.
